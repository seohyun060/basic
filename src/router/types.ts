// type Access = {
//   loggedInOnly?: boolean; // 로그인 회원만 접근 가능
//   notLoggedInOnly?: boolean; // 비로그인 회원만 접근 가능
//   doctorOnly?: boolean; // 의사 회원만 접근 가능
// };

// export type Route = {
//   fullPath: string;
//   relativePath: string;
//   meta?: MetaData;
//   access?: Access;
// };

// export type MetaData = {
//   $darktheme?: BoolsByDevice;
//   $beigetheme?: BoolsByDevice;
//   footer?: BoolsByDevice;
// };

// export type BoolsByDevice = {
//   pc: boolean;
//   mobile: boolean;
// };
